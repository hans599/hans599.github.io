---
title: RAG与向量数据库
date: 2026-06-11
tags: RAG
mathjax: true
---

# 前言：为什么需要RAG？

- RAG，检索增强生成（Retrieval-Augmented Generation），RAG 是一种将外部知识库检索与 LLM 生成相结合的技术架构，旨在解决大模型的知识截止、幻觉、无法访问私有数据等核心问题。


<!-- more --> 

- RAG vs 微调，者不是"二选一"的关系，而是互补的技术，各有优劣。


```py
用户需求
    │
    ▼
┌─────────────────────────────────────┐
│ 是否需要访问实时/私有/外部知识？     │
└─────────────────────────────────────┘
    │                    │
   是                   否
    │                    │
    ▼                    ▼
┌──────────────┐   ┌──────────────────┐
│   用 RAG     │   │ 是否需要改变     │
│             │   │ 模型的风格/能力？  │
└──────────────┘   └──────────────────┘
                         │          │
                        是         否
                         │          │
                         ▼          ▼
                  ┌──────────┐  ┌──────────┐
                  │  微调    │  │ 普通 LLM │
                  └──────────┘  └──────────┘

常见组合：
- 知识问答 → RAG
- 风格化客服 → 微调 + RAG
- 代码生成 → 微调
- 内部助手 → 微调（高频问题）+ RAG（实时政策）

```

- 本博客目标：上传PDF → 检索相关内容 → LLM生成回答


# RAG核心知识


- RAG（检索增强生成）由三个核心模块组成：

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                           RAG 三大核心模块                                   │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│   ┌─────────────────┐    ┌─────────────────┐    ┌─────────────────┐        │
│   │   ① 检索模块     │    │   ② 增强模块     │    │   ③ 生成模块     │        │
│   │   (Retrieve)    │───▶│   (Augment)     │───▶│   (Generate)    │        │
│   ├─────────────────┤    ├─────────────────┤    ├─────────────────┤        │
│   │ • 嵌入模型       │    │ • Prompt 拼接    │    │ • LLM 大模型     │        │
│   │ • 向量数据库     │    │ • 上下文窗口     │    │ • 流式输出       │        │
│   │ • 检索器         │    │ • 相关性过滤     │    │ • 答案生成       │        │
│   └─────────────────┘    └─────────────────┘    └─────────────────┘        │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```


| 模块         | 组件          | 作用                                                  |
| :----------- | :------------- | :----------------------------------------------------- |
| ① 检索模块 | 嵌入模型     | 将文本转换为向量（数学表示）                      |
|              | 向量数据库   | 存储和索引向量（Chroma、Pinecone、Weaviate） |
|              | 检索器        | 根据问题找到最相关的文档片段                      |
| ② 增强模块 | Prompt 拼接 | 将检索到的文档与原始问题组合                      |
|              | 上下文窗口   | 控制输入给 LLM 的内容长度                         |
|              | 相关性过滤   | 剔除不相关的检索结果                               |
| ③ 生成模块 | LLM 大模型   | 基于增强后的 Prompt 生成答案                     |
|              | 流式输出     | 实时返回生成结果                                    |
|              | 答案溯源     | 标注答案来源（引用文档）                           |



- 简要工作流程
```
PDF文档 → 分块(Chunking) → 向量化(Embedding) → 存入向量数据库
                    ↑
用户提问 → 向量化 → 相似度检索 → 召回相关块 → 拼接Prompt → LLM生成回答
```

- 五个核心步骤：加载 → 分块 → 嵌入 → 检索 → 生成



- RAG 核心公式

```py
# RAG 的数学表示
Answer = LLM(
    Prompt(
        Query,                    # 用户问题
        Retrieve(Query, KB)       # 从知识库检索的相关文档
    )
)

# 简化版
RAG(question) = LLM(question + search(question, knowledge_base))
```



# Chunking策略（分块）


原始文档通常很长（如一本书、一份财报），无法一次性全部输入 LLM。分块（Chunking）有两个核心目的：

| 目的 | 说明 |
| :--- | :--- |
| **适配上下文窗口** | LLM 有输入长度限制（如 4K、8K、128K tokens），分块后每次只送入相关片段 |
| **提升检索精度** | 小块更容易精确匹配用户问题，避免大段无关内容干扰 Embedding |

```text
不分块的问题：
┌─────────────────────────────────────────────┐
│ 整本书（100万 tokens）                       │
│ 问：主角叫什么？ → Embedding 被大量无关内容稀释 │
│ 结果：检索不到准确答案                        │
└─────────────────────────────────────────────┘

分块后：
┌──────────┐ ┌──────────┐ ┌──────────┐
│ 第1章    │ │ 第2章    │ │ ...      │
│ 主角出场  │ │ 情节发展  │ │          │
└──────────┘ └──────────┘ └──────────┘
问：主角叫什么？ → 精确匹配到第1章 → 准确回答
```

常见分块策略对比：

| 策略         | 说明                                                     | 优点             | 缺点            | 适用场景               |
| :----------- | :------------------------------------------------------- | :--------------- | :-------------- | :-------------------- |
| 固定大小分块 | 按固定字符数或 token 数切分                               | 简单、快速       | 会切断句子/段落 | 快速原型、简单文档     |
| 递归分块     | 按段落 → 句子 → 字符的优先级切分，尽量保持语义完整         | 保持自然边界     | 稍复杂          | 通用推荐               |
| 语义分块     | 利用 Embedding 计算句子间相似度，在语义变化处切分          | 语义完整性最好   | 计算开销大      | 高质量要求、长文档     |
| 重叠分块     | 块与块之间有重叠部分（如前一块的后10%与下一块的前10%重叠） | 避免边界信息丢失 | 增加存储和计算  | 重要文档、边界敏感任务 |

代码示例，用 LangChain 的 RecursiveCharacterTextSplitter
```py
from langchain.text_splitter import RecursiveCharacterTextSplitter

# 示例文档
document = """
第一章：AI 的起源

人工智能（AI）的概念最早可以追溯到 1950 年代。图灵测试是评估机器智能的经典方法。

第二章：机器学习

机器学习是 AI 的核心分支。深度学习通过多层神经网络实现了重大突破。

第三章：应用场景

AI 已广泛应用于图像识别、自然语言处理和推荐系统等领域。
"""

# 配置分块器
text_splitter = RecursiveCharacterTextSplitter(
    chunk_size=64,        # 每块最大字符数（实际使用建议 512-1024）
    chunk_overlap=20,      # 块之间重叠字符数（约 10-20%）
    separators=["\n\n", "\n", "。", "，", " ", ""],  # 按优先级切分
    length_function=len,   # 长度计算函数
)

# 执行分块
chunks = text_splitter.split_text(document)

# 查看结果
for i, chunk in enumerate(chunks):
    print(f"Chunk {i}: {chunk}\n")
```


输出为
```
Chunk 0: 第一章：AI 的起源

人工智能（AI）的概念最早可以追溯到 1950 年代。图灵测试是评估机器智能的经典方法。

Chunk 1: 第二章：机器学习

机器学习是 AI 的核心分支。深度学习通过多层神经网络实现了重大突破。

第三章：应用场景

Chunk 2: 第三章：应用场景

AI 已广泛应用于图像识别、自然语言处理和推荐系统等领域。

```

分块策略选择指南：
```text
我的文档是...
    │
    ├── 简单的纯文本、日志 → 固定大小分块
    │
    ├── 文章、报告、书籍 → 递归分块（推荐）
    │
    ├── 技术文档、学术论文 → 递归分块 + 重叠
    │
    └── 追求极致效果 → 语义分块
    
```


# Embedding原理


Embedding 是将文本转换为**固定长度的向量**（一串数字）的技术，让计算机能够理解和比较文本的语义。

```text
文本 → Embedding 模型 → 向量（如 768 维）

"苹果很好吃" → [0.12, -0.34, 0.56, ..., 0.78]  (768个数字)
"香蕉很香甜" → [0.11, -0.32, 0.55, ..., 0.79]  (语义相似的向量)
"今天天气不错" → [0.45, 0.23, -0.12, ..., 0.31]  (语义不同的向量)

相似度计算：苹果向量 · 香蕉向量 ≈ 0.95（高）
          苹果向量 · 天气向量 ≈ 0.32（低）
```


MTEB 的全称是 Massive Text Embedding Benchmark（大规模文本嵌入基准测试）。它是目前业界公认的、最全面、最权威的文本嵌入模型（Embedding Model）评估排行榜。
[Hugging face MTEB leaderboard](https://huggingface.co/spaces/mteb/leaderboard)这里有很多开源的嵌入模型。
一些示例模型如下：

| 模型系列               | 示例模型                                                     |
| :-------------------- | :---------------------------------------------------------- |
| BAAI/bge              | BAAI/bge-large-zh-v1.5                                      |
| Qwen3-Embedding       | Qwen/Qwen3-Embedding-0.6B                                   |
| sentence-transformers | sentence-transformers/paraphrase-multilingual-MiniLM-L12-v2 |
| text2vec              | shibing624/text2vec-large-chinese                           |
| m3e                   | moka-ai/m3e-base                                            |

评估指标解释如下：
| 任务类型 | 中文含义 | 说明 | 应用场景 |
| :--- | :--- | :--- | :--- |
| Classification | 分类 | 判断文本属于哪个类别 | 垃圾邮件识别、情感分析、新闻分类 |
| Clustering | 聚类 | 将相似文本自动分组 | 客户分群、话题聚合、重复文档检测 |
| PairClassification | 配对分类 | 判断两段文本是否属于同一类别 | 重复问题检测、文本匹配 |
| Reranking | 重排序 | 对检索结果进行二次精排 | 搜索引擎优化、RAG 检索结果优化 |
| Retrieval | 检索 | 从海量文档中找到最相关的文档 | RAG 核心指标、信息检索、问答系统 |
| STS | 语义文本相似度 | 计算两段文本的语义相似程度 | 文本去重、相似度匹配、抄袭检测 |

代码示例：使用  [Qwen3-Embedding-0.6B](https://huggingface.co/Qwen/Qwen3-Embedding-0.6B)
```py
from sentence_transformers import SentenceTransformer
import numpy as np

# 本地离线模型
model = SentenceTransformer("D:\\Devs\\local_models\\Qwen\\Qwen3-Embedding-0.6B", local_files_only=True)

# 模拟：文档被切分成不同长度的 chunks
chunks = [
    "北京",                                          # 2个字
    "中国的首都是北京。",                             # 10个字
    "北京是中华人民共和国的首都，位于华北平原北部。",    # 20个字
    "北京拥有悠久的历史，是中国的政治、文化中心..."     # 50个字
]

# 用户 Query（长短不一）
queries = [
    "首都",           # 2个字
    "中国的首都是哪里？",  # 10个字
    "请告诉我中国的首都是什么城市？"  # 15个字
]

# 编码所有 chunks（向量维度固定）
chunk_embeddings = model.encode(chunks)
print(f"Chunks 向量形状: {chunk_embeddings.shape}")  # (4, 1024)

# 编码 queries
for query in queries:
    query_embedding = model.encode([query])
    print(f"\nQuery: '{query}' -> 向量形状: {query_embedding.shape}")  # (1, 1024)
    
    # 计算相似度
    similarities = model.similarity(query_embedding, chunk_embeddings)[0]
    for chunk, sim in zip(chunks, similarities):
        print(f"  与 '{chunk[:20]}...' 相似度: {sim:.4f}")
```
输出结果
```py
Loading weights: 100%|███████████████████████████████████████████████████████████████████████████████████████████████████████████████████████████████| 310/310 [00:00<00:00, 7353.43it/s]
Chunks 向量形状: (4, 1024)

Query: '首都' -> 向量形状: (1, 1024)
  与 '北京...' 相似度: 0.8138
  与 '中国的首都是北京。...' 相似度: 0.4935
  与 '北京是中华人民共和国的首都，位于华北平原...' 相似度: 0.5135
  与 '北京拥有悠久的历史，是中国的政治、文化中...' 相似度: 0.5845

Query: '中国的首都是哪里？' -> 向量形状: (1, 1024)
  与 '北京...' 相似度: 0.5195
  与 '中国的首都是北京。...' 相似度: 0.8563
  与 '北京是中华人民共和国的首都，位于华北平原...' 相似度: 0.6677
  与 '北京拥有悠久的历史，是中国的政治、文化中...' 相似度: 0.5789

Query: '请告诉我中国的首都是什么城市？' -> 向量形状: (1, 1024)
  与 '北京...' 相似度: 0.4823
  与 '中国的首都是北京。...' 相似度: 0.7029
  与 '北京是中华人民共和国的首都，位于华北平原...' 相似度: 0.5896
  与 '北京拥有悠久的历史，是中国的政治、文化中...' 相似度: 0.5089
```

原理示意图
```
文档切分（Chunking）：
┌─────────────────────────────────────────┐
│ 原始文档（5000字）                        │
└─────────────────────────────────────────┘
                    ↓ 切分
┌──────────┐ ┌──────────┐ ┌──────────┐
│ Chunk 1  │ │ Chunk 2  │ │ Chunk 3  │
│ (200字)  │ │ (180字)  │ │ (220字)  │
└────┬─────┘ └────┬─────┘ └────┬─────┘
     ↓            ↓            ↓
  Embedding    Embedding     Embedding
     ↓            ↓            ↓
┌──────────┐ ┌──────────┐ ┌──────────┐
│ 向量 V1  │ │ 向量 V2  │ │ 向量 V3  │
│ (1024维) │ │ (1024维) │ │ (1024维) │
└──────────┘ └──────────┘ └──────────┘
                          ↑
用户 Query: "中国的首都在哪里？" (10字)
                          ↓
                      Embedding
                          ↓
                    ┌──────────┐
                    │ 向量 Vq  │
                    │ (1024维) │
                    └──────────┘
                          ↓
              计算余弦相似度: Vq · V1, V2, V3
                          ↓
                   找到最相似的 Chunk
```


# 向量数据库

向量数据库是专门用于存储和检索向量数据的数据库系统。

在 RAG 系统中，向量数据库扮演着“外部大脑”的角色：

```
原始文档 → Embedding 模型 → 向量 → 存入向量数据库
                                    ↓
用户问题 → Embedding 模型 → 查询向量 → 相似度检索 → 返回相关文档
```

核心能力：

- 向量存储：高效存储高维向量（如 768 维、1024 维）

- 相似度检索：通过余弦相似度、欧氏距离等算法，快速找到最相似的向量

- 向量索引：使用 HNSW、IVF 等算法加速检索，从“遍历所有向量”优化为“近似最近邻搜索”


主流向量数据库对比:

| 数据库    | 特点                      | 适用场景                 |
| :------- | :------------------------ | :---------------------- |
| Chroma   | 轻量级、嵌入式、零配置     | 本地开发、学习、原型验证 |
| Qdrant   | 高性能、云原生、过滤功能强 | 生产环境、中等规模       |
| Pinecone | 全托管、免运维、有免费额度 | 不想自己运维、快速上线   |
| Milvus   | 分布式、功能全面、开源     | 大规模生产（亿级向量）   |



Chroma 代码示例
```py
from langchain_community.embeddings import OllamaEmbeddings
from langchain_community.vectorstores import Chroma
from langchain_core.documents import Document

# ========== 1. 准备文档 ==========
documents = [
    Document(
        page_content="公司报销流程：填写报销单 → 主管审批 → 财务审核 → 出纳打款",
        metadata={"category": "财务"}
    ),
    Document(
        page_content="请假制度：需提前3天申请，紧急情况可电话请示",
        metadata={"category": "人事"}
    ),
    Document(
        page_content="考勤规则：工作时间 9:00-18:00，迟到需说明原因",
        metadata={"category": "人事"}
    ),
]

# ========== 2. 初始化 Embedding 模型 ==========
embeddings = OllamaEmbeddings(
    model="qwen3-embedding:0.6b",
    base_url="http://localhost:11434"
)

# ========== 3. 创建向量存储 ==========
vectorstore = Chroma.from_documents(
    documents=documents,
    embedding=embeddings,
    persist_directory="./langchain_chroma_db",   # 向量数据库保存目录
)

# ========== 4. 相似度检索 ==========
query = "怎么报销"
results = vectorstore.similarity_search(query, k=2)

print(f"🔍 查询：{query}")
for doc in results:
    print(f"   📄 {doc.page_content}")

# ========== 5. 带分数的检索 ==========
results_with_score = vectorstore.similarity_search_with_score(query, k=2)
for doc, score in results_with_score:
    print(f"   余弦距离: {score:.4f} | {doc.page_content[:30]}...")

# ========== 6. 转为检索器 ==========
retriever = vectorstore.as_retriever(search_kwargs={"k": 3})
docs = retriever.invoke("请假")
```

输出结果：
```text
🔍 查询：怎么报销
   📄 公司报销流程：填写报销单 → 主管审批 → 财务审核 → 出纳打款
   📄 请假制度：需提前3天申请，紧急情况可电话请示
   余弦距离: 0.6790 | 公司报销流程：填写报销单 → 主管审批 → 财务审核 → 出...
   余弦距离: 1.0836 | 请假制度：需提前3天申请，紧急情况可电话请示...
```

`similarity_search_with_score` 原型，根据描述可知输出的是余弦距离，值越小，相似度越高。
```py
    def similarity_search_with_score(
        self,
        query: str,
        k: int = DEFAULT_K,
        filter: Optional[Dict[str, str]] = None,
        where_document: Optional[Dict[str, str]] = None,
        **kwargs: Any,
    ) -> List[Tuple[Document, float]]:
        """Run similarity search with Chroma with distance.

        Args:
            query (str): Query text to search for.
            k (int): Number of results to return. Defaults to 4.
            filter (Optional[Dict[str, str]]): Filter by metadata. Defaults to None.

        Returns:
            List[Tuple[Document, float]]: List of documents most similar to
            the query text and cosine distance in float for each.
            Lower score represents more similarity.
        """
```

值得注意的是，第一次运行会创建`langchain_chroma_db`目录，但如果重复运行，会重复的把相同的documents向量保存。所以实际运用时要注意去重处理。


- Chroma 核心操作速查表

| 操作 | 代码 | 说明 |
| :--- | :--- | :--- |
| 创建客户端 | `chromadb.PersistentClient(path="./db")` | 持久化存储 |
| 创建 Collection | `client.create_collection(name="my_docs")` | 创建表 |
| 插入文档 | `collection.add(documents=[...], ids=[...])` | 批量插入 |
| 检索 | `collection.query(query_embeddings=[...], n_results=3)` | 向量检索 |
| 过滤检索 | `collection.query(..., where={"field": "value"})` | 按元数据过滤 |
| 查看数量 | `collection.count()` | 文档总数 |
| 删除 Collection | `client.delete_collection("my_docs")` | 删除表 |

- 关键点总结

| 概念        | 说明                      |
| :--------- | :------------------------ |
| 向量数据库  | 专门存储和检索向量的数据库 |
| Collection | 类似关系数据库中的表       |
| Document   | 存储的文本内容             |
| Metadata   | 附加的标签/属性，用于过滤  |
| 相似度检索  | 找到与查询向量最相似的文档 |
| 持久化      | 数据保存到磁盘，重启不丢失 |

# 实战：搭建文档问答Agent


## 整体架构设计
```py
┌─────────────────────────────────────────────────────────────────────────────┐
│                        文档问答 Agent 整体架构                               │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│   ┌─────────────┐                                                          │
│   │  PDF/文档    │                                                          │
│   │  (原始文件)  │                                                          │
│   └──────┬──────┘                                                          │
│          │                                                                  │
│          ▼                                                                  │
│   ┌─────────────┐     ┌─────────────┐     ┌─────────────┐                  │
│   │  文档加载    │ ──▶ │  文档分块    │ ──▶ │  向量化存储  │                  │
│   │ (PyPDFLoader)│     │ (Recursive) │     │ (Chroma)   │                  │
│   └─────────────┘     └─────────────┘     └──────┬──────┘                  │
│                                                    │                        │
│                                                    ▼                        │
│                                         ┌─────────────────────┐            │
│                                         │   向量数据库 (持久化) │            │
│                                         └─────────────────────┘            │
│                                                                             │
│   ┌─────────────────────────────────────────────────────────────────────┐   │
│   │                       在线阶段                                       │   │
│   ├─────────────────────────────────────────────────────────────────────┤   │
│   │                                                                      │   │
│   │  用户问题 ──▶ ┌─────────────┐                                        │   │
│   │              │  检索模块    │──▶ 相似度检索 ──▶ Top-K 相关文档        │   │
│   │              └──────┬──────┘                                        │   │
│   │                     │                                               │   │
│   │                     ▼                                               │   │
│   │              ┌─────────────┐                                        │   │
│   │              │  生成模块    │──▶ 构建 Prompt ──▶ LLM 生成答案       │   │
│   │              └─────────────┘                                        │   │
│   │                                                                      │   │
│   └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```


## 步骤一：加载 PDF 文档

1. 安装依赖
```bash
pip install pypdf langchain-community chromadb sentence-transformers
```

2. 加载 PDF 文档

```py
import re
from langchain_community.document_loaders import PyPDFLoader

# 加载单个 PDF
loader = PyPDFLoader("E:\\codes\\my-llm-learning\\海外人力模块操作手册.pdf")
documents = loader.load()


print(f"✅ 成功加载 {len(documents)} 页")
print(f"   第一页内容预览: {documents[0].page_content[:100]}...")
```

输出
```
✅ 成功加载 126 页
   第一页内容预览: 海外⻔⼾-DHR&SF员⼯中⼼模块操作⼿册-中⽂
版
OverseasPortal-DHR-SFEmployeeCenterModuleOperationManual-En...
```

3. 加载多种格式

```
from langchain_community.document_loaders import TextLoader, UnstructuredMarkdownLoader

def load_documents(file_path: str):
    """根据文件扩展名自动选择加载器"""
    if file_path.endswith('.pdf'):
        loader = PyPDFLoader(file_path)
    elif file_path.endswith('.txt'):
        loader = TextLoader(file_path, encoding='utf-8')
    elif file_path.endswith('.md'):
        loader = UnstructuredMarkdownLoader(file_path)
    else:
        raise ValueError(f"不支持的文件格式: {file_path}")
    
    return loader.load()

# 使用示例
# docs = load_documents("documents/README.md")
```


##  步骤二：文档分块

```py
from langchain_text_splitters import RecursiveCharacterTextSplitter

# 配置分块参数
text_splitter = RecursiveCharacterTextSplitter(
    chunk_size=500,          # 每块最大 500 字符
    chunk_overlap=50,        # 块与块重叠 50 字符
    separators=["\n\n", "\n", "。", "！", "？", "；", "，", " ", ""],
    length_function=len,
)

# 执行分块
chunks = text_splitter.split_documents(documents)

print(f"📝 原始文档: {len(documents)} 页")
print(f"📄 分块后: {len(chunks)} 个块")
print(f"   平均块大小: {sum(len(c.page_content) for c in chunks) / len(chunks):.0f} 字符")

# 观察分块效果
print("\n🔍 分块示例:")
for i, chunk in enumerate(chunks[:3]):
    print(f"   --- 块 {i+1} ---")
    print(f"   {chunk.page_content[:150]}...")
    print()
```

输出
```
📝 原始文档: 126 页
📄 分块后: 120 个块
   平均块大小: 154 字符

🔍 分块示例:
   --- 块 1 ---
   海外⻔⼾-DHR&SF员⼯中⼼模块操作⼿册-中⽂
版
OverseasPortal-DHR-SFEmployeeCenterModuleOperationManual-EnglishVersion
⾼频问题速查...

   --- 块 2 ---
   加载失败，请重试
1. 系统基础信息
1.1 海外⻔⼾登录信息
1. ⽅式⼀：直接登录海外⻔⼾
•
⽣产地址：https://wwww
•
账号：00XXXXXX/密码
•
密码重置：点击"忘记密码"→通过⻜书⼯号...

   --- 块 3 ---
   1.2 SF员⼯中⼼登录说明+语⾔切换
...
如需切换为简体中⽂，请按以下路径操作：
•
点击SF⻚⾯右上⻆头像→选择“设置”→在“语⾔”选项中选择“简体中...
```
  
乱码的问题可能是PDF本身自带的一些特殊符号没有被解析，也有可能是jupyter本身打印格式的问题，后续再来解决，先把整个流程跑通。
  

## 步骤三：创建向量数据库
```py
from sentence_transformers import SentenceTransformer
import chromadb
import numpy as np

class LocalEmbeddingFunction:
    """自定义 Embedding 函数，使用本地 SentenceTransformer（兼容 Chroma 0.5.0+）"""
    
    def __init__(self, model_path: str):
        self.model = SentenceTransformer(model_path, local_files_only=True)
    
    def __call__(self, input):  # ← 参数名必须为 'input'
        """Chroma 要求的接口签名: def __call__(self, input)"""
        embeddings = self.model.encode(input, convert_to_numpy=True)
        return embeddings.tolist()

class VectorStore:
    def __init__(self, persist_path: str = "./chroma_db", collection_name: str = "documents"):
        self.client = chromadb.PersistentClient(path=persist_path)
        self.collection_name = collection_name
        
        # 使用本地 SentenceTransformer
        self.embedding_fn = LocalEmbeddingFunction("D:\\Devs\\local_models\\Qwen\\Qwen3-Embedding-0.6B")
        
        try:
            self.client.delete_collection(collection_name)
        except:
            pass
        
        self.collection = self.client.create_collection(
            name=collection_name,
            embedding_function=self.embedding_fn
        )
        print(f"✅ 向量数据库初始化完成 (使用本地模型)")
    
    def add_documents(self, chunks, batch_size: int = 50):
        """批量插入文档"""
        total = len(chunks)
        for i in range(0, total, batch_size):
            batch = chunks[i:i+batch_size]
            self.collection.add(
                documents=[doc.page_content for doc in batch],
                ids=[f"chunk_{i+j}" for j in range(len(batch))],
                metadatas=[
                    {"source": doc.metadata.get("source", ""), "page": doc.metadata.get("page", 0)}
                    for doc in batch
                ]
            )
            print(f"   已插入 {min(i+batch_size, total)}/{total}")
        print(f"✅ 全部 {total} 个块已存入向量数据库")
    
    def check_exists(self) -> bool:
        return self.collection.count() > 0
        
# 使用示例
vector_store = VectorStore(persist_path="./chroma_db", collection_name="company_handbook")
vector_store.add_documents(chunks)
```
运行结果
```
Loading weights: 100%|██████████| 310/310 [00:00<00:00, 2026.20it/s]
✅ 向量数据库初始化完成 (使用本地模型)
   已插入 50/120
   已插入 100/120
   已插入 120/120
✅ 全部 120 个块已存入向量数据库
```

持久化保存（下次启动无需重建）
```py

# 检查是否已有数据
def get_or_create_vectorstore(persist_path: str = "./chroma_db", collection_name: str = "documents"):
    client = chromadb.PersistentClient(path=persist_path)
    
    # 检查 collection 是否存在且非空
    try:
        collection = client.get_collection(name=collection_name)
        if collection.count() > 0:
            print(f"✅ 加载已有向量数据库，包含 {collection.count()} 条文档")
            return collection
    except:
        pass
    
    # 不存在则创建新的
    collection = client.create_collection(name=collection_name)
    print(f"📝 创建新的向量数据库")
    return collection
```


## 步骤四：实现检索模块


```py
# 使用示例
# 1. 加载或创建向量数据库
collection = get_or_create_vectorstore(
    persist_path="./chroma_db", 
    collection_name="company_handbook"
)

retrieved = retrieve_documents(
    query="怎么请假？",
    collection=collection,
    top_k=3,
    threshold=0.1
)

# 打印结果
print("\n📄 检索结果:")
for r in retrieved:
    print(f"   📄 相似度: {r['similarity']:.3f}")
    print(f"      {r['content'][:100]}...")
    print()
    
```

输出
```
✅ 加载已有向量数据库，包含 120 条文档
🔍 检索: '怎么请假？'
   召回 3 条，过滤后 3 条

📄 检索结果:
   📄 相似度: 0.319
      导航路径：假勤管理-考勤管理-请假申请
销假
导航路径：假勤管理-考勤管理-请假申请...

   📄 相似度: 0.286
      请假/销假
请假...

   📄 相似度: 0.219
      填写/编辑补卡申请表单
外出/销外出
外出申请
导航路径：假勤管理-考勤管理-外出申请...
```  
  


## 步骤五：实现生成模块



构建Prompt模板（包含上下文 + 问题）, 以便调用LLM生成答案并引用来源
```py
from langchain_openai import ChatOpenAI
from langchain_core.messages import HumanMessage, SystemMessage
from sentence_transformers import SentenceTransformer
import chromadb

OLLAMA_BASE_URL = "http://localhost:11434/v1"
OLLAMA_API_KEY = "ollama"
LLM_MODEL = "qwen2.5:3b"

llm = ChatOpenAI(
    base_url=OLLAMA_BASE_URL,
    api_key=OLLAMA_API_KEY,
    model=LLM_MODEL,
    temperature=0
)

# ========== 构建 Prompt ==========
def build_prompt(query: str, retrieved_docs: list) -> str:
    """构建增强 Prompt"""
    context = ""
    for i, doc in enumerate(retrieved_docs, 1):
        context += f"[来源 {i}]\n{doc['content']}\n\n"
    
    return f"""请根据以下参考资料回答用户问题。

【参考资料】
{context}

【用户问题】
{query}

【回答要求】
1. 如果参考资料中有完整信息，给出详细回答
2. 如果参考资料只有部分信息（如菜单路径），请告诉用户如何操作，并说明这是从参考资料中找到的
3. 如果参考资料完全没有相关信息，才告知"未找到相关信息"
4. 回答要准确、简洁、有条理
5. 引用来源时标注 [来源 n]

【回答】
"""

# ==========  生成答案 ==========
def generate_answer(query: str, retrieved_docs: list) -> str:
    """生成最终答案"""
    if not retrieved_docs:
        return "❌ 未找到相关信息，请尝试换个问题。"
    
    prompt = build_prompt(query, retrieved_docs)
    response = llm.invoke([
        SystemMessage(content="你是一个专业的文档问答助手，擅长基于给定资料回答用户问题。"),
        HumanMessage(content=prompt)
    ])
    return response.content

# 加载数据库
collection = get_or_create_vectorstore(
    persist_path="./chroma_db",
    collection_name="company_handbook"
)

# 检查是否有数据
if collection.count() == 0:
    print("⚠️ 数据库为空，请先插入文档！")
    exit(1)


```
使用示例
```py
# 用户问题
query = "我想请假的话，怎么请？"

# 检索
retrieved = retrieve_documents(query, collection, top_k=3, threshold=0.1)

# 生成
answer = generate_answer(query, retrieved)

# 输出
print(f"\n📄 检索结果:")
for r in retrieved:
    print(f"   相似度: {r['similarity']:.3f} | {r['content'][:80]}...")

print(f"\n🤖 最终答案:\n{answer}")

```

输出结果
```
✅ 加载已有向量数据库，包含 120 条文档
🔍 检索: '我想请假的话，怎么请？'
   召回 3 条，过滤后 3 条

📄 检索结果:
   相似度: 0.270 | 导航路径：假勤管理-考勤管理-请假申请
销假
导航路径：假勤管理-考勤管理-请假申请...
   相似度: 0.232 | 请假/销假
请假...
   相似度: 0.179 | 出差变请到PC端处理
加班...

🤖 最终答案:
根据您的问题，您想了解请假的具体流程。从参考资料来看，[来源 1] 提供了详细的导航路径：假勤管理 -> 考勤管理 -> 请假申请。

因此，您可以按照以下步骤操作：
- 首先找到假勤管理的入口。
- 然后在假勤管理中选择考勤管理。
- 最后，在考勤管理下选择请假申请来提交您的请假申请。

这是从参考资料中找到的操作方法。[来源 1]
```


加上循环之后就能做成交互式的了。




# 常见问题与调试技巧

Q1: 检索不到相关内容怎么办？

调整分块大小、换Embedding模型、降低阈值

Q2: 回答出现幻觉怎么办？

增强Prompt约束、提高召回相关性、加“不知道”选项

Q3: 中文效果不好怎么办？

换中文专用Embedding模型（BGE、m3e）

Q4: PDF解析乱码怎么办？

换OCR方案、预处理清洗



# 总结

## RAG 核心流程回顾


```
┌─────────────────────────────────────────────────────────────────────────────┐
│                         RAG 完整流程                                        │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│   ┌─────────────────────────────────────────────────────────────────────┐   │
│   │                        离线阶段（索引）                               │   │
│   ├─────────────────────────────────────────────────────────────────────┤   │
│   │                                                                      │   │
│   │   PDF/TXT/MD ──▶ 文档加载 ──▶ 文档分块 ──▶ Embedding ──▶ 向量数据库  │   │
│   │                     (Loader)    (Splitter)    (Model)    (Chroma)    │   │
│   │                                                                      │   │
│   └─────────────────────────────────────────────────────────────────────┘   │
│                                       │                                     │
│                                       ▼                                     │
│   ┌─────────────────────────────────────────────────────────────────────┐   │
│   │                        在线阶段（问答）                               │   │
│   ├─────────────────────────────────────────────────────────────────────┤   │
│   │                                                                      │   │
│   │   用户问题 ──▶ 向量检索 ──▶ 相关文档 ──▶ 构建Prompt ──▶ LLM生成答案   │   │
│   │              (Retrieve)   (Top-K)   (Augment)   (Generate)          │   │
│   │                                                                      │   │
│   └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```


## 下一步可以做什么
1. 加入对话记忆（支持多轮）

```py
from langgraph.checkpoint.memory import MemorySaver

# 在 Agent 中添加记忆
memory = MemorySaver()
app = workflow.compile(checkpointer=memory)

# 使用 thread_id 区分会话
config = {"configurable": {"thread_id": "user_001"}}
```

效果：Agent 能记住用户之前问过什么，实现多轮对话。


2. 支持多种文档格式
```py
def load_documents(file_path: str):
    """根据文件类型自动选择加载器"""
    if file_path.endswith('.pdf'):
        loader = PyPDFLoader(file_path)
    elif file_path.endswith('.txt'):
        loader = TextLoader(file_path, encoding='utf-8')
    elif file_path.endswith('.md'):
        loader = UnstructuredMarkdownLoader(file_path)
    elif file_path.endswith('.docx'):
        from langchain_community.document_loaders import Docx2txtLoader
        loader = Docx2txtLoader(file_path)
    elif file_path.endswith('.csv'):
        from langchain_community.document_loaders import CSVLoader
        loader = CSVLoader(file_path)
    else:
        raise ValueError(f"不支持的文件格式: {file_path}")
    return loader.load()
```
    

3. 接入 LangGraph 做复杂路由


```py
from langgraph.graph import StateGraph, END

# 定义路由：根据问题类型选择不同的处理路径
def router(state):
    query = state["query"]
    
    # 检测问题类型
    if "报销" in query or "请假" in query:
        return "rag"  # 走 RAG 路径
    elif "你好" in query or "再见" in query:
        return "chat"  # 走闲聊路径
    else:
        return "rag"  # 默认走 RAG

# 构建图
workflow = StateGraph(State)
workflow.add_node("rag", rag_node)      # RAG 处理
workflow.add_node("chat", chat_node)    # 闲聊处理
workflow.add_conditional_edges("router", router, {
    "rag": "rag",
    "chat": "chat"
})
```


| 优化点   | 方法                                     |
| :------- | :--------------------------------------- |
| 检索质量 | 混合检索（关键词+向量）、重排序（Rerank） |
| 回答质量 | 更好的 Prompt、多轮对话、追问机制         |
| 性能     | 异步处理、缓存、批量 Embedding            |
| 多文档   | 路由、多向量库管理                        |



# 完整代码链接
[https://github.com/hans599/my-llm-learning/blob/main/03-RAG/doc_rag.ipynb](https://github.com/hans599/my-llm-learning/blob/main/03-RAG/doc_rag.ipynb)