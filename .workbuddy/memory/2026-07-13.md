# 2026-07-13 工作日志

## 音视频博客系列（source/_posts/音视频/）
- 新建《音视频理论基石：从数字信号到编解码的核心概念》后经两轮展开：
  - 1.2 采样定理：补"奈奎斯特频率/混叠 → 抗混叠滤波器过渡带余量 → 为何选 44.1k/48k（非理论最小 40k）"推导链
  - 2.1 色彩空间：补"为何不用 RGB 而用 YUV"——人眼重亮度轻色度(视杆1.2亿/视锥700万)、RGB 降采样伤亮度(G 权重 0.587)、历史兼容黑白电视
- 新建《I帧、P帧、B帧：视频压缩的核心机制》(20260713-视频帧类型与压缩核心机制.md)
  - 含三种帧对比表、P 帧 4 步拆解(底图→MV→残差→存说明书，带 SAD/残差/重建公式)、B 帧双向预测、PTS vs DTS、DPB 重排序、错误传播与 IDR、mermaid 依赖图 + 全链路图、参考资料
- 开启 _config.next.yml 的 mermaid.enable: true，使上述 mermaid 图能在页面渲染（已验证 mermaid.min.js 注入、图文本写入 HTML）
