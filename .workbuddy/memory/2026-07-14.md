# 2026-07-14 工作日志

## 新增博客文章
- 文件：`source/_posts/音视频/20260714-FFmpeg环境搭建与命令行实战.md`
- 主题：FFmpeg 环境搭建（Windows 用 BtbN 的 `-shared` 版）+ 命令行实战（解复用/解码/编码/封装闭环）
- 已补两幅图：① 容器文件内部结构 ASCII 方框图（Header/Data + ftyp/index/NAL/AAC）；② 解复用→解码→编码→封装流水线 ASCII 图（配 mermaid 流程图）
- 含官方测试视频资源：samples.ffmpeg.org、Blender demo movies、test-videos.co.uk
- 校验：front-matter 合法、代码围栏配对(34)、hexo g 不报错（但本沙箱缺 hexo-generator-post，不产生文章 HTML，已记入 MEMORY.md）

## 约定更新
- 用户要求：博客标题不要手加 `一、二、三` 等中文序号（也不要 `2.1` 手动编号），因为 Hexo/NexT 渲染会自动编号。已把 FFmpeg 文章全部 13 个标题的序号前缀去掉，并将该规则写入 MEMORY.md 长期约定。
- 用户要求：改完博客不要帮我部署/发布，只直接编辑 md 源文件即可，部署与发布用户自行手动处理。已写入 MEMORY.md。
